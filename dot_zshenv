# Adding python pip user-space binaries
export PATH="$PATH:$HOME/.local/bin"

# Docker-compose support for buildkit
export BUILDKIT_PROGRESS=plain

# Vault
export VAULT_ADDR=https://vault.s73cloud.com/
